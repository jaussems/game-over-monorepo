import { ViewtransitionDirective } from './viewtransition.directive';

describe('ViewtransitionDirective', () => {
  it('should create an instance', () => {
    const directive = new ViewtransitionDirective();
    expect(directive).toBeTruthy();
  });
});
